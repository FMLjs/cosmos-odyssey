import React from 'react';

interface Props {
};

export const Reservations: React.FC<Props> = (props) => {
    const {
    } = props;
    
    return (
        <div className='reservations'>
            hi, here is you can find reservation
        </div>
    );
}